# HR-Schema-Analysis
HR Schema Analysis – SQL Project
This project analyzes a standard HR database using SQL to extract insights on employees, departments, jobs, and salaries. It demonstrates the use of joins, aggregations, and subqueries to solve real-world HR business questions.
The project demonstrates the use of core SQL concepts such as:
SELECT statements
WHERE conditions
JOINs (INNER, LEFT)
GROUP BY and HAVING
Subqueries
Aggregate functions
